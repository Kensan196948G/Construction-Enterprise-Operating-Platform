"""conftest for security scan tests.

No shared fixtures are required for the subprocess-based scan tests,
but this file marks the directory as a pytest package root and may be
extended with fixtures (e.g., a virtualenv snapshot) in the future.
"""
