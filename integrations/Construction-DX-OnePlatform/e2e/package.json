{
  "name": "@cdx/e2e",
  "version": "0.1.0",
  "private": true,
  "description": "Construction DX One Platform — Playwright E2E スイート",
  "scripts": {
    "e2e": "playwright test",
    "e2e:ui": "playwright test --ui",
    "e2e:chromium": "playwright test --project=chromium",
    "e2e:install": "playwright install --with-deps",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "@types/node": "^22.7.0",
    "typescript": "^5.6.2"
  }
}
