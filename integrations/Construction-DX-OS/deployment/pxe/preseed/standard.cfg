#### Construction-DX-OS — Standard Profile preseed
# Issue 0042 Phase 4.2
#
# Target: 情シス / 管理者端末 (事務所 LAN 接続)
# Locale/keyboard/timezone are set for Japan.

# --- Locale ---
d-i debian-installer/locale string ja_JP.UTF-8
d-i keyboard-configuration/xkb-keymap select jp
d-i time/zone string Asia/Tokyo
d-i clock-setup/utc boolean true
d-i clock-setup/ntp boolean true

# --- Network ---
# DHCP from PXE — no extra config needed.
# Hostname prefix will be overridden by agent-bootstrap.sh post-install.
d-i netcfg/choose_interface select auto
d-i netcfg/get_hostname string cdx-standard
d-i netcfg/get_domain string local

# --- Mirror ---
d-i mirror/country string JP
d-i mirror/http/hostname string deb.debian.org
d-i mirror/http/directory string /debian
d-i mirror/http/proxy string

# --- Account ---
# Root account disabled; sudo user created instead.
d-i passwd/root-login boolean false
d-i passwd/user-fullname string CDX Admin
d-i passwd/username string cdxadmin
# Password hash: change before production (mkpasswd -m sha-512)
d-i passwd/user-password-crypted password $6$rounds=500000$placeholder$CHANGEME_BEFORE_PRODUCTION

# --- Disk ---
d-i partman-auto/disk string /dev/sda
d-i partman-auto/method string lvm
d-i partman-auto-lvm/guided_size string max
d-i partman-auto/choose_recipe select atomic
d-i partman-partitioning/confirm_write_new_label boolean true
d-i partman/choose_partition select finish
d-i partman/confirm boolean true
d-i partman/confirm_nooverwrite boolean true

# --- Packages ---
tasksel tasksel/first multiselect standard
d-i pkgsel/include string openssh-server curl wget gnupg2 apt-transport-https ca-certificates

# --- Boot loader ---
d-i grub-installer/only_debian boolean true
d-i grub-installer/bootdev string /dev/sda

# --- Post-install: fetch and run agent-bootstrap.sh via late_command ---
# CDX_SERVER_URL is passed as kernel cmdline (cdx_server=...) by iPXE.
# agent-bootstrap.sh handles mTLS token fetch + cdx-agent installation.
d-i preseed/late_command string \
    in-target /bin/bash -c ' \
        CDX_PROFILE="standard"; \
        CDX_SERVER=$(cat /proc/cmdline | tr " " "\n" | grep "^cdx_server=" | cut -d= -f2-); \
        CDX_SERVER="${CDX_SERVER:-http://192.168.0.185:8300}"; \
        curl -fsSL "${CDX_SERVER}/pxe/agent-bootstrap.sh" \
            -o /tmp/agent-bootstrap.sh; \
        chmod +x /tmp/agent-bootstrap.sh; \
        CDX_PROFILE="${CDX_PROFILE}" \
        CDX_SERVER_URL="${CDX_SERVER}" \
        /tmp/agent-bootstrap.sh; \
    '

# --- Finish ---
d-i finish-install/reboot_in_progress note
