[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.backends._legacy:_Backend"

[project]
name = "construction-enterprise-os-auth-core"
version = "0.1.0"
description = "Construction-Enterprise-OS shared authentication middleware"
dependencies = [
    "fastapi>=0.100",
    "python-jose[cryptography]>=3.3",
    "pydantic>=2.0",
]
